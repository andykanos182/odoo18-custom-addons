<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ============================================== -->
        <!-- Cron: Auto-check pending DOKU transactions    -->
        <!-- Runs every 15 minutes to sync transactions    -->
        <!-- that may have missed webhook notifications    -->
        <!-- ============================================== -->
        <record id="ir_cron_doku_check_pending_transactions" model="ir.cron">
            <field name="name">DOKU: Check Pending Transactions</field>
            <field name="model_id" ref="payment.model_payment_transaction"/>
            <field name="state">code</field>
            <field name="code">model._cron_doku_check_pending_transactions()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

        <!-- ============================================== -->
        <!-- Cron: Auto-expire old pending transactions    -->
        <!-- Runs every hour to mark expired transactions  -->
        <!-- ============================================== -->
        <record id="ir_cron_doku_expire_old_transactions" model="ir.cron">
            <field name="name">DOKU: Expire Old Pending Transactions</field>
            <field name="model_id" ref="payment.model_payment_transaction"/>
            <field name="state">code</field>
            <field name="code">model._cron_doku_expire_old_transactions()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
